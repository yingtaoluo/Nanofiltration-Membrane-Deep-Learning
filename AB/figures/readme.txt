"alone" represents the loss/pearson evaluation with different datasets during training process.
"correlation" indicates the bias from prediction value to real value with different datasets.
"together" compares train/test/valid loss/pearson of all datasets.